const chai = require('chai');
const PeerConnection = require('../src/peerConnection');

describe('Connections', function() {
    it('should connect, send and receive data');
    it('should connect and disconnect with error management');
    it('should fail on incorrect peer address');
    it('should fail on non-json string');
});
